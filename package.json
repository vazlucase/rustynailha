{
  "name": "rusty-na-ilha",
  "version": "2.0.0",
  "private": true,
  "description": "Site institucional + sistema de reservas + admin do Rusty na Ilha (Vercel + Postgres)",
  "scripts": {
    "create-admin": "node scripts/create-admin.js",
    "init": "node scripts/migrate.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}